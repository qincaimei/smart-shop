server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/qincaimei_shop?erverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: Bigdata22
    driver-class-name: com.mysql.cj.jdbc.Driver

  thymeleaf:
    cache: true

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.glu.smartshop.entity
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.glu.smartshop: warn